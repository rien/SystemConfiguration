---
dependencies:
  - { role: letsencrypt_domain,
      letsencrypt_domain: 'test.rxn.be' }

  - { role: nginx_vhost,
      nginx_vhost_file: 'test',
      nginx_vhost_location: 'roles/test_letsencrypt/files' }
